int main ()
{
      int a, b;
      float c, d, e;
      char f;
      double variavel;
      a = b * c;
      c = b - (d + 1);

      f++;
      --e;
      if( a > b && b==5)
      {
            c=1*2-4/c;
      }
      else
      {
            while(d > 0)
            {
                     d--;
            }
      }
      do{
           b = a-f*(b+c);
      }while(f != 10);
      for(int i = 0; i < 200; i++)
      {
             a += b - 4*(e + 5) / (i+1);
      }
      return 0;
}
